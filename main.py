import numpy as np
import pandas as pd

df = pd.DataFrame({
    "Languages" : ["Brazilian Portuguese", "Deutsch", "French", "German", "Hungarian", "Italian", "Korean",
                 "Modern Standard Arabic", "Spanish", "Russian"],
})

for (i) in range(len(df_preselected_mentees)):{
(set(df_preselected_mentees.iloc[1-33]).intersection(df_preselected_mentors.iloc[1-11]))

}
